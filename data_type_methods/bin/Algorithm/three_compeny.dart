void main(List<String> args) {
  var w = 'Hello';
  var output = '';
  for (int i = 0; i < w.length; i++) {
    // print(w[i] + w[i] + w[i]);
    output += w[i] + w[i] + w[i];
  }
  print(output);
  toRevesre();
}

toRevesre() {
  var inp = 9956;
  var outp = '';
  for (int i = 0; i < inp; i++) {
    print(outp);
  }
}
